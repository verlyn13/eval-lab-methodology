"""Experimental method-tranche computations excluded from the released core.

These modules support a decision-input analysis package. They are deliberately
kept below ``analysis/`` so neither the versioned public-core import surface nor
the ``eval-lab-methodology`` wheel can acquire them accidentally.
"""
